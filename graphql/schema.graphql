type Movie {
    name: String!
    score: Int!
    id: Int!
}

type Query {
    movies: [Movie]!
    movie(id: Int!): Movie
}

type Mutation {
    deleteMovie(id: Int!): Boolean
    addMovie(name: String!, score:Int!): Movie!
}